

	glBindBuffer(GL_ARRAY_BUFFER, colorbuffer);
	//printf("Cor: %lu", sizeof(g_color_pernas_hulk));
	//printf("XY: %lu", sizeof(pernas_hulk));
	//printf("Calculado: %lu", ((sizeof(pernas_hulk)/4)+(sizeof(pernas_hulk)/4)/2)*4);
	GLfloat *aux = colorir(pernas_hulk, 'v');
	glBufferData(GL_ARRAY_BUFFER, ((sizeof(pernas_hulk)/4)+(sizeof(pernas_hulk)/4)/2)*4, aux, GL_STATIC_DRAW);
	glBindBuffer(GL_ARRAY_BUFFER, vertexbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(pernas_hulk), pernas_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(pernas_hulk));
	free(aux);

	glBindBuffer(GL_ARRAY_BUFFER, colorbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(g_color_calcao_hulk), g_color_calcao_hulk, GL_STATIC_DRAW);
	glBindBuffer(GL_ARRAY_BUFFER, vertexbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(calcao_hulk), calcao_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(calcao_hulk));


	glBindBuffer(GL_ARRAY_BUFFER, colorbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(g_color_corpo_hulk), g_color_corpo_hulk, GL_STATIC_DRAW);
	glBindBuffer(GL_ARRAY_BUFFER, vertexbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(torax_hulk), torax_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(torax_hulk));
	glBufferData(GL_ARRAY_BUFFER, sizeof(bracos_hulk), bracos_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(bracos_hulk));
	glBufferData(GL_ARRAY_BUFFER, sizeof(cabeca_hulk), cabeca_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(cabeca_hulk));


	glBindBuffer(GL_ARRAY_BUFFER, colorbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(g_color_cabelo_hulk), g_color_cabelo_hulk, GL_STATIC_DRAW);
	glBindBuffer(GL_ARRAY_BUFFER, vertexbuffer);
	glBufferData(GL_ARRAY_BUFFER, sizeof(cabelo_hulk), cabelo_hulk, GL_STATIC_DRAW);
	glDrawArrays(GL_TRIANGLES, 0, sizeof(cabelo_hulk));
